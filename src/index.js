import { functionCall } from "./index2.js";

document.addEventListener("submit", functionCall);
export const todo ={};
export  var todoq = "<ul>{{Task_Name}} {{Start_date}} {{Category}}{{Due_Date}}</ul>"
